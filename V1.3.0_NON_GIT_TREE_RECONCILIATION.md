# v1.3.0 Non-Git Tree Reconciliation

## Sources

- Authoritative repository: the Git-backed `v1.2.5-hotfix/nmap-flow-analyzer`
  checkout, preserved with its complete `.git` history.
- Authoritative checkpoint: commit
  `4882fcdebd74076bd35d30de8798574294c7dd69`.
- Reference tree inspected:
  `nmap-flow-analyzer-v1.3.0/nmap-flow-analyzer` from the 2026-07-22
  handoff workspace.
- Reconciliation branch: `release/v1.3.0-linux-rc`.

The reference tree was inspected recursively by relative path and SHA-256
content hash. Transient cache, virtual-environment, build, distribution, and
bytecode patterns were excluded from the comparison. None were present.

## Inventory and classification

The entire reference tree contains two files and one empty directory.

### Identical files

| Path | SHA-256 | Decision |
| --- | --- | --- |
| `requirements.txt` | `8b4c4217a0054ce381d1371aadddbf8925aae136651dc483243ed7bf3f2e8008` | Already present; no import needed. |
| `TEST_RESULTS.txt` | `2e8b217bca649d5c2ff84afb41cf6a3a71a74e419e610024b6f79f5c555b1fba` | Already present; no import needed. |

### New files

None.

### Modified files

None.

### Conflicting files

None.

### Generated or transient files ignored

None were present.

### Empty directories

- `nmap_flow_analyzer/` was empty. Git does not track empty directories, and
  the authoritative repository already contains the real package directory,
  so there is nothing to import.

## Imported and rejected work

No files were imported because the only files in the reference tree are
byte-identical to authoritative files. No meaningful files were rejected.
The reference tree contains no `pyproject.toml`, build scripts, preflight or
update implementation, platform packaging, CI workflows, or v1.3.0-specific
documentation.

## Behavioral implications

The reconciliation changes no analyzer behavior, schemas, evidence
correlation, confidence scoring, firewall candidates, or output safety. The
authoritative Git repository remains the sole source of implementation
history.

Before reconciliation, an approved Windows portability correction was added
on the reconciliation branch to close an explicitly opened Nmap XML input on
all parser exit paths. This corrects a real file-handle leak without changing
successful parsing or analysis decisions.

## Test implications

There is no imported implementation to test. After the approved portability
correction:

- `tests/test_logging_lifecycle.py`: 8 passed.
- Full pytest suite: 397 passed, 14 skipped.
- Offline test runner: passed.
- `compileall` for `nmap_flow_analyzer`: passed.

The skipped pytest cases are environment-dependent optional-dependency tests;
they are not represented as passes. Linux platform verification and the
v1.2.4-to-v1.2.5 compatibility comparison remain separate required gates.
